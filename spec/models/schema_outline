trndr2

	user
		user_id
		email
		password
		has many media through favorites
	


	media
		media_id
		title
		artist
		type (song, video, movie, photo)
		source (iTunes, Youtube, etc.)
		country
		URL

	favorites
		user_id <=> media_id
